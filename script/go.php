<?php

//Retry img from ftp image
  $dir = '../imgftp';
  echo (count(glob("$dir/*")) === 0) ? 'Empty' : 'Not empty';

//Copy img to current directory

//Delete img from ftp image

//Archive the old image

?>
